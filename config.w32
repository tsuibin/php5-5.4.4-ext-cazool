// $Id$
// vim:ft=javascript

// If your extension references something external, use ARG_WITH
// ARG_WITH("cazool", "for cazool support", "no");

// Otherwise, use ARG_ENABLE
// ARG_ENABLE("cazool", "enable cazool support", "no");

if (PHP_CAZOOL != "no") {
	EXTENSION("cazool", "cazool.c");
}

